import Title from "../components/TitleSection/Title";

export const ROUTE_CONSTANTS = {
  home: {
    path: "/",
    pageToRender: "title",
    showHeader: true,
    showFooter: true,
  },
};

export const Components = {
  title: Title,
};
